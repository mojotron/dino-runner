function GroundView() {
  const ground = document.querySelector('.ground-wrapper');
  const position = 0;
  let timerId;

  const move = () => {};
  const stop = () => {};

  return { move };
}

export default GroundView();
